<h2>コメント</h2>

<%= form_for (@comment) do |f| %>
  <div class="field">
    <%= f.label :content, "コメント" %><br />
    <%= f.text_area :content %>
  </div>

  <div class="actions">
    <%= f.submit "コメントする" %>
  </div>
<% end %>
